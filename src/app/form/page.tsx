import { ProfileForm } from "@/components/pers/Form";

export default function Form() {
    return (
        <div className="flex flex-col items-center justify-center min-h-screen p-4">
            <ProfileForm></ProfileForm>
        </div>
    );
}